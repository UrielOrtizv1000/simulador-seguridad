version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # One PR for the workflow actions instead of one per action.
    groups:
      actions:
        patterns: ["*"]
